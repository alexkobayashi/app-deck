<?xml version="1.0" encoding="utf-8"?>
<!--
  Tema mínimo apenas para a janela antes do Compose assumir. Herda de
  android:Theme.Material para não precisar da dependência
  com.google.android.material, que o app não usa.
-->
<resources>
    <style name="Theme.AppDeck" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/deck_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
